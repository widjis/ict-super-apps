services:
  backend:
    build:
      context: ./backend
      target: dev
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      CORS_ORIGIN: "*"
    volumes:
      - ./backend:/app
      - /app/node_modules
